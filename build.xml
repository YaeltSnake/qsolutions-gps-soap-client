<?xml version="1.0" encoding="UTF-8"?>
<project name="GPSWebServicesClient" default="default" basedir=".">
    <description>Builds, tests, and runs the project GPSWebServicesClient.</description>
    <import file="nbproject/build-impl.xml"/>

    <target name="-pre-jar">
        <copy file="${src.dir}/logback.xml" todir="${build.classes.dir}"/>
    </target>

</project>
